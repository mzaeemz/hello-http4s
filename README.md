# Hello World App for http4s

To run API locally, in terminal type:

```bash
sbt run
```


### API wil listen at localhost:8080

For Hello Message visit:
> localhost:8080/hello/name

For a Dad Joke visit:
> localhost:8080/joke